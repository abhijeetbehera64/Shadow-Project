pipeline {
    agent any
    stages {
	 stage ('PreBuild') {
            steps {
                script {
                    bat "cd VehicleManagement && dotnet restore"
                    bat "cd VehicleManagement && dotnet build -t:SyntaxCheck"
                }
            }
        }
        stage ('Build') {
                steps {
                    script {
                        bat 'cd VehicleManagement && dotnet build'
                    }
                }
            }
        }
    }
