pipeline {
    agent any
    stages {
        stage ('Build') {
                steps {
                    script {
                        bat 'cd VehicleManagement && dotnet build'
                    }
                }
            }
        }
    }
